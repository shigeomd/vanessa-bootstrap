&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ПричинаЗапросаКода) Тогда
		Элементы.ПричинаЗапросаКода.Заголовок = Параметры.ПричинаЗапросаКода;
	КонецЕсли;
	Данные64КартинкиСКодом = Параметры.Данные64КартинкиСКодом;
	ДанныеКартинкиСКодом = Base64Значение(Данные64КартинкиСКодом);
	КартинкаСКодом = ПоместитьВоВременноеХранилище(ДанныеКартинкиСКодом);
	
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	
	Закрыть(Код);
	
КонецПроцедуры
