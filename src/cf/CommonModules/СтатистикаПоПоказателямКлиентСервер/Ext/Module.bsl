////////////////////////////////////////////////////////////////////////////////
// Подсистема "СтатистикаПоПоказателям"
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

Процедура ДобавитьСобытие(ИмяСобытия, Комментарий = "") Экспорт
	
	РазделениеВключено = Ложь;
	
	#Если Клиент Тогда
		РазделениеВключено = СтандартныеПодсистемыКлиентПовтИсп.ПараметрыРаботыКлиента().РазделениеВключено;	
	#Иначе
		РазделениеВключено = ОбщегоНазначенияПовтИсп.РазделениеВключено();
	#КонецЕсли
		
	Если Не РазделениеВключено Тогда
		Возврат;
	КонецЕсли;	
	
	#Если Клиент Тогда	
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			ИмяСобытиеСМаркером(ИмяСобытия), "Информация", Комментарий);		
	#Иначе
		ЗаписьЖурналаРегистрации(
			ИмяСобытиеСМаркером(ИмяСобытия), УровеньЖурналаРегистрации.Информация,,, Комментарий);
	#КонецЕсли
	
КонецПроцедуры

Функция МаркерСобытияСтатистики() Экспорт
	
	Возврат "Статистика_abd4d044ff5798fa";
               
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ИмяСобытиеСМаркером(ИмяСобытия)
	
	Возврат МаркерСобытияСтатистики() + ". " + ИмяСобытия;
	
КонецФункции

#КонецОбласти